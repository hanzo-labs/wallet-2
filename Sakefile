use('sake-bundle')
use('sake-outdated')
use('sake-publish')
use('sake-version')

task('build', 'Build the project into a static page', () => {
  exec('next build && next export')
})

task('watch', 'Start local watch server', () => {
  exec('next')
})

